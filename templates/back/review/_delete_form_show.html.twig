<form method="post" class ="col-1" action="{{ path('app_back_review_delete', {'id': review.id}) }}" onsubmit="return confirm('Etes vous sur de vouloir supprimer cet article?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ review.id) }}">
        <button class="btn btn-danger col-12 mt-1">Supprimer</button>
</form>
