<form method="post" class="col-1" action="{{ path('app_back_comment_delete', {'id': comment.id}) }}" onsubmit="return confirm('Etes vous sur de vouloir supprimer ce commentaire ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ comment.id) }}">
        <button class="btn btn-danger col-12 mt-1">Supprimer</button>
</form>