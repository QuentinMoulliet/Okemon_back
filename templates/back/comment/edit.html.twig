{% extends 'base.html.twig' %}
{% form_theme form 'bootstrap_5_layout.html.twig' %}
{% block title %}Admin | Modification{% endblock %}

{% block body %}
    <div class="container">
     <h1 class="text-center mt-4 text-white mb-5">Modification du commentaire</h1>
    
    {{ include('back/comment/_form.html.twig', {'button_label': 'Modifier'}) }}
    {{ include('back/comment/_delete_form.html.twig') }}

     <div class="d-flex justify-content-center">
        <a href="{{ path('app_back_comment_index') }}" class="btn btn-secondary mt-1 col-2 mb-5" role="button">Retour à la liste</a>
    </div>
{% endblock %}
