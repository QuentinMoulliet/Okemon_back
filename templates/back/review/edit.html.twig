{% extends 'base.html.twig' %}
{% form_theme form 'bootstrap_5_layout.html.twig' %}
{% block title %}Admin | Modification{% endblock %}

{% block body %}
{% include "back/fragments/_flash_message.html.twig" %}
    <div class="container">
     <h1 class="text-center mt-4 mb-5">Modification de l'article {{review.title}}</h1>

    {{ include('back/review/_form.html.twig', {'button_label': 'Modifier'}) }}
    {{ include('back/review/_delete_form.html.twig') }}

    <div class="d-flex justify-content-center">
        <a href="{{ path('app_back_review_index') }}" class="btn btn-secondary mt-1 col-2 mb-5" role="button">Retour à la liste</a>
    </div>
{% endblock %}
